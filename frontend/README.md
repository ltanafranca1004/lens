# Lens — frontend

React + TypeScript + Vite SPA for [Lens](../README.md). See the root README for project overview, architecture, setup, and API docs.

Quick start: `npm install && npm run dev` (proxies `/auth` and `/sessions` to `localhost:8000`).
